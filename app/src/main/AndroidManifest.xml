<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zzw.customview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".QQSportStepViewActivity" />
        <activity android:name=".JumpLoadViewActivity" />
        <activity android:name=".AlphabeticalIndexViewActivity" />
        <activity android:name=".ColorTrackTextViewActivity" />
        <activity android:name=".ViewPagerActivity" />
        <activity android:name=".RatingBarActivity" />
        <activity android:name=".TagLayoutActivity" />

        <activity android:name=".MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".TestActivity">


        </activity>
    </application>

</manifest>